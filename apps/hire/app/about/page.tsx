import Link from "next/link";
import { site } from "@/content/site";
import { getBookingDestination } from "@/lib/booking";
export const metadata = { title: "About", description: "How Animesh Sharma works across product, engineering, and operations.", alternates: { canonical: "/about" } };
export default function AboutPage() { const booking = getBookingDestination(); return <main id="main-content" className="v2-about"><div className="site-shell"><p className="section-kicker">About / {site.person}</p><h1>One person, close to the work.</h1><p className="v2-lead">I work across product, engineering, and operations. I like the ambiguous part: finding the actual problem, shaping a useful system, and getting it into people’s hands.</p><div className="v2-about-grid"><div><h2>Direct collaboration</h2><p>You work with the person making the product and engineering decisions. I keep the scope, constraints, and open questions visible.</p></div><div><h2>Useful over elaborate</h2><p>A first version should help people do something real. I start with the workflow and choose the smallest build that can be tested.</p></div></div><div className="hero-actions"><a className="button" href={booking.href} rel={booking.configured ? "noreferrer" : undefined} target={booking.configured ? "_blank" : undefined}>{booking.configured ? "Book a call" : "Request a call"}</a><Link className="text-link" href="/work">See what I’ve built</Link></div></div></main>; }
